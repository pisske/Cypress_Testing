{
  "name": "cypress-automation-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "trigerAllTests-headless": "npx cypress run",
    "triggerAllTest-headed": "npx cypress run --headed",
    "triggerAllTest-chrome": "npx cypress run --browser chrome",
    "triggerAllTest-dashboard": "npx cypress run --record --key"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.4.1",
    "cypress-file-upload": "^5.0.8",
    "cypress-xpath": "^2.0.0"
  },
  "dependencies": {
    "chai": "^4.3.6"
  }
}
